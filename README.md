To-Do List CLI App (Python)
This is a simple command-line To-Do List application built in Python. It allows users to:

Add tasks

View all tasks

Mark tasks as completed

Delete tasks

Save tasks to a tasks.json file for persistence

All tasks are stored in a JSON file, making the data persistent across sessions. This script is beginner-friendly and demonstrates basic file handling, user input, and JSON manipulation in Python.
